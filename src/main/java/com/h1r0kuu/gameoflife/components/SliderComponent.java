package com.h1r0kuu.gameoflife.components;

import javafx.scene.control.Slider;

public class SliderComponent extends Slider {
    public SliderComponent(double min, double max, double initialValue) {
        super(min, max, initialValue);
    }
}
